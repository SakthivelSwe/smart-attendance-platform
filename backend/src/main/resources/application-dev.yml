# Development profile - uses Supabase PostgreSQL for local testing
spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres
    username: postgres.npppkkmatyleuopiyyaa
    password: "Luffy#zoro56"
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      keepalive-time: 60000
      validation-timeout: 5000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # Google OAuth credentials
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1080249198691-444ld2o9fqo8qdm76usdmie92lk8knjh.apps.googleusercontent.com
            client-secret: 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

  # Disable mail in dev
  mail:
    host: localhost
    port: 1025
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

# Dev JWT secret
app:
  jwt:
    secret: DevSecretKeyForLocalTestingOnly256BitsMinimumRequiredForHS256Algorithm2024!
    expiration-ms: 86400000


  admin:
    emails: sakthiveltony@gmail.com

  cors:
    allowed-origins: http://localhost:4200,http://localhost:3000

logging:
  level:
    com.smartattendance: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
